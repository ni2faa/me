apiVersion: v1
kind: Service
metadata:
  name: me-svc
  namespace: default
spec:
  selector:
    app: me               # must match your Deployment's pod label
  ports:
    - name: http
      port: 80            # service port
      targetPort: 3000    # containerPort in your pod
      protocol: TCP
  type: ClusterIP
