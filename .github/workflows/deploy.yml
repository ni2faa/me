name: Docker Build and Push

on:
  push:
    branches:
      - main
      - feat/infra

env:
  REGISTRY_HOST_IP: ${{ secrets.REGISTRY_HOST_IP }}      # or read from secrets if you want
  REGISTRY_PORT: 5000
  REGISTRY_HOST: ${{ secrets.REGISTRY_HOST_IP }}:5000
  IMAGE_NAME: ${{ github.repository }} # like ni2faa/me

jobs:
  build-and-push:
    runs-on: self-hosted
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set image tag
        id: vars
        run: |
          TAG="${GITHUB_SHA::7}"
          echo "TAG=$TAG" >> "$GITHUB_OUTPUT"

      - name: Debug tags
        run: |
          echo "REGISTRY_HOST=${{ env.REGISTRY_HOST }}"
          echo "IMAGE_NAME=${{ env.IMAGE_NAME }}"
          echo "TAG=${{ steps.vars.outputs.TAG }}"
          echo "FULL: ${{ env.REGISTRY_HOST }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.TAG }}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY_HOST }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.TAG }}
            ${{ env.REGISTRY_HOST }}/${{ env.IMAGE_NAME }}:latest

